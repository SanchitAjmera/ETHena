from src import process
import analysis
# initial variables


class Trader:

    def __init__(self, initialFund, initialInventory, initialDay, givenData, testData):
        self.funds = initialFund
        self.inventory = initialInventory
        self.day = initialDay
        self.historicalData = givenData
        self.testData = testData

    def trade():
        if


trader1 = Trader(100000, 0, 0, process.historicalData, process.testData)
